# Mason frontend integration audit — 10 September 2026

Inspected canonical `tarangdoshi/mason-web` main `b84f056`, companion `tarangdoshi/mason-api` checkout `2f5d3d5`, and Prerna's `prernaa-99/Mason-company-` commit `670a599399d022aaedda779b20fa63cf7e556497`. Existing web worktree is dirty and will not be modified. API is a separate repository/deployment.

1. **Existing routes/functionality:** `/`, `/about`, `/compare-packages`, `/packages/standard`, `/packages/advanced`, `/checkout/[packageId]`, `/evidence`, `/communications`, `/operations/technician-readiness`, `/privacy`, `/terms`; staff `/crm`, login, lead detail, embedded Sanity, legacy `/admin`. Next handlers proxy guidance/checkout and CRM, and protect Sanity draft mode. Homepage CMS has published/fallback/draft reads. Quiz, GA4, CTA/session/UTM attribution, validated phone/email and real submission errors already exist.
2. **New routes/components:** `/`, `/about`, `/packages`, `/why`, `/contact`, `/privacy`, `/terms`, `/refund`. Nav, Hero, Stats, Safer + inline VisitForm, transformations, packages/advisor, process, doctors, testimonials, FAQ, booking dialog, footer, legal layout; GSAP/Lenis motion and scroll restoration.
3. **Direct-deployment losses:** booking/contact only call console.info and falsely show success; location returns null. No API routes, CRM, checkout, CMS/drafts, analytics, persisted quiz/attribution, sitemap or robots protection. Existing deep links would 404.
4. **Framework/dependencies:** canonical Next 15.5.12 / React 19.1.1 / Node 20 / pnpm 10.16.1; design Next 16.2.11 / React 19.2.4 / Tailwind 4 / GSAP / @gsap/react / Lenis / ESLint 9 with npm lock. Keep canonical framework/runtime; add only design/build/lint dependencies actually required. No backend dependency upgrade.
5. **Integration points:** preserve POST `/api/leads/guidance` → Fastify `/api/v1/public/leads/guidance`, canonical validation/error handling and duplicate-submit gate; preserve checkout endpoint separately. Booking UI must retain assessment type, notes, privacy notice and location metadata, and only show success after real API success. Contact enquiry must use guidance API. Keep canonical quiz accessible and do not label the new advisor as the old quiz version.
6. **Assets/fonts/styles:** 29 source public assets include logos, before/after, bathrooms, portraits and framework placeholders. Migrate used brand/photos under a namespace, omit unused framework placeholders. Archivo/Fraunces italic/Geist/Geist Mono replace public typography. Scope Tailwind reset, tokens and design CSS to public presentation to avoid affecting CRM/admin and retained pages. Preserve mobile and reduced-motion behaviour. Native dialog top layer needs special care with Google's body-mounted autocomplete dropdown.
7. **SEO/meta:** preserve existing route metadata, robots exclusions and sitemap routes; add new routes and canonical/OG metadata. New source has basic titles/descriptions only. Keep Preview noindex. Retain canonical legal text rather than publish source registered-address/GST placeholders and unapproved legal promises.
8. **Environment:** existing `NEXT_PUBLIC_API_URL` (real API), `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, `NEXT_PUBLIC_GA_MEASUREMENT_ID`, `NEXT_PUBLIC_SANITY_PROJECT_ID`, `NEXT_PUBLIC_SANITY_DATASET`, `NEXT_PUBLIC_SANITY_API_VERSION`; server-only `SANITY_API_READ_TOKEN`, `SANITY_PREVIEW_SECRET`. API retains its existing database/auth/integration configuration and server-only `GOOGLE_MAPS_API_KEY`. No secrets in source. Preview must target an identified non-production API/database for test leads; missing Google must leave submission usable.
9. **Deployment risks:** missing standalone lockfile; Vercel project scope, Preview env, API connectivity, Google referrer permissions, CSS leakage, animation hydration, missing assets, unknown production baseline, public env baked at build. Vercel connector scope returns 403, CLI authenticates successfully; verify team/project with CLI. Use isolated feature branch → PR → Git-backed Preview; no merge or Production deploy in this task.
10. **Conflicting assumptions:** new forms imply a booked visit without creating any record; visits must remain ENQUIRY, including Goa/Bangalore/OTHER/UNKNOWN. New mobile validation only checks length. New advisor scores a different questionnaire and stores nothing. New location is optional while canonical UI marks address required and API requires a non-empty string: remove browser gating and send an explicit address-not-provided placeholder with UNKNOWN metadata to meet the explicit requirement. New copy says Bengaluru/Goa and promises future-city notification; use “Mason is currently available in Goa and Bangalore.” Canonical CMS functionality and existing package/checkout contracts must remain accessible rather than silently disappear. New legal placeholders must not replace working policy pages.

Implementation approach: port the reviewed design into the canonical web repository, retain operational routes and backend contracts, wire existing lead capture into the new visual surfaces, retain canonical quiz/CMS paths, and document all remaining content/editorial differences. No database migration is planned.
