/* Mason's real contact details, in one place. The footer and the contact page
   both publish the email, and they drifted onto two different domains once
   already (two different domains at once) - a reader had no
   way to tell which was real. Import from here rather than typing them out. */

/** Customer support email, published on the contact page, footer and legal pages. */
export const CARE_EMAIL = "support@masoncompany.in";

/** Grouped 5-5 for display, matching the mobile placeholder in both forms. */
export const PHONE_DISPLAY = "+91 81494 33383";
/** Unspaced - a tel: href with spaces in it will not dial. */
export const PHONE_HREF = "tel:+918149433383";
export const WHATSAPP_URL = "https://wa.me/918149433383";

/** Customer support hours, as approved by the founder. */
export const HOURS = "Monday to Friday, 10 am to 7 pm";
